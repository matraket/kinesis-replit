# CMS / Domain

Modelos de vista, reglas de UI del backoffice y mapeos entre DTOs del API y modelos de presentación. Contiene la lógica de negocio específica del CMS. NO debe contener componentes React ni llamadas HTTP (van en `presentation` e `infrastructure`).
