# CMS / Infrastructure

Adaptadores técnicos del CMS: HTTP clients hacia `/api`, storage del browser, servicios de autenticación. Implementa los puertos definidos en `application`. NO debe contener lógica de negocio (va en `domain`) ni componentes React (van en `presentation`).
