# API / Infrastructure

Implementaciones de repositorios, adaptadores de DB (Replit Database), servicios externos, mappers ORM, Replit Auth y Replit App Storage. Adapta proveedores externos a interfaces neutrales definidas en `application`. NO debe contener lógica de negocio (va en `domain`) ni controladores HTTP (van en `interfaces`).
