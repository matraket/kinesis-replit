# Shared / Types

Tipos TypeScript y DTOs compartidos entre frontend y backend: interfaces de contratos API, enums comunes, tipos de respuesta HTTP. NO debe contener tipos específicos de dominio del backend (van en `api/domain`) ni modelos de vista del frontend.
