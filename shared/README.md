# Shared

Código reutilizable entre `web`, `cms` y `api` (no confundir con `core`). Contiene librerías de componentes UI, utilidades generales y tipos/DTOs compartidos frontend-backend. NO debe contener lógica de negocio específica de módulos ni eventos de dominio.
