Analiza el siguiente PRD exhaustivo que define la siguiente tarea (T8).
Debes elaborar un PRD sintetizado en el que incorpores la información relevante para que el Agent pueda llevar a cabo la tarea de forma exitosa y en la primera iteración. No omitas las restricciones impuestas pues estan incorporadas con el objetivo de que el Agent no se extralimite en sus funciones. **CRITICO** No se debe de perder nada de información. Simplemente debe compactarse para que el Agent pueda digerirla y manejarla correctamente
Genera el PRD en una caja de markdown para que pueda copiarlo facilmente. Si creas dentro del tu respuesta cajas de código como

**CRITICO**
Si abres un bloque de código usando 

Ejemplo:
```typescript
// Ejemplo
```

**Asegurate de cerrar el bloque con los tres acentos pues no las cierras y se rompe la caja de markdown. 
Verifica que la extension del PRD generado no vaya a suponer un problema para el Agent a la vez que verificas que no hayas omitido información relevante