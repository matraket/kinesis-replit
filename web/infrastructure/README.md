# Web / Infrastructure

Adaptadores técnicos: HTTP clients hacia `/api`, storage del browser (localStorage, sessionStorage), servicios externos. Implementa los puertos definidos en `application`. NO debe contener lógica de negocio (va en `domain`) ni componentes React (van en `presentation`).
