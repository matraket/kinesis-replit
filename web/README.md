# Web

Frontend público de Kinesis (React + Vite + Tailwind). Sigue arquitectura por capas (domain, application, infrastructure, presentation). Se comunica con el backend exclusivamente a través de HTTP hacia `/api`. NO debe tener lógica de backend ni acceso directo a base de datos.
