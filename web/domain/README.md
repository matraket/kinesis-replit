# Web / Domain

Modelos de vista, reglas de UI ligeras y mapeos entre DTOs del API y modelos de presentación. Contiene la lógica de negocio específica del frontend público. NO debe contener componentes React ni llamadas HTTP (van en `presentation` e `infrastructure`).
